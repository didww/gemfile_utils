# Licenses: ["MIT"]
source 'https://rubygems.org'

# Specify your gem's dependencies in gemfile_desc.gemspec
gemspec

group :test do
  # (MIT) Record your test suite's HTTP interactions and replay them during future test runs for fast, deterministic, accurate tests.
  gem 'vcr'
end


